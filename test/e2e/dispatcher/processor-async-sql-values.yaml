processor:
  config:
    dispatchMode: async
    modelGateways: null
    globalInferenceGateway: null
    asyncDispatch:
      transport: sql
      resultPollTimeout: "30s"
      models:
        sim-model:
          inferencePoolName: "sim-pool"
        sim-model-b:
          inferencePoolName: "sim-pool-b"
        sim-model-gate:
          inferencePoolName: "sim-pool-gate"
        sim-model-inject:
          inferencePoolName: "sim-pool-inject"
